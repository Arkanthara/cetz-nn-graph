name: ci

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: typst-community/setup-typst@v1
      - run: bash scripts/render-examples.sh
      - run: typst compile --root . docs/guide.typ /tmp/guide.pdf
